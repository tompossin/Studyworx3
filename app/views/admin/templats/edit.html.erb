<% content_for :breadcrumbs do %>
  <%=link_to "Template Home", admin_school_templats_path(@school) %>
<% end %>
<div id="content-large">
  <article>
    
    <h1>Editing <%= @templat.name %></h1>

    <%= render "admin/templats/form"%>
  </article>
</div>

<%= nav_bar(@nav_body_content) %>

<%= smart_edit(@templat.content_type, admin_school_templat_path(@school,@templat)) %>

