<%= form_for(@user_admin) do |f| %>
  <% if @user_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_admin.errors.count, "error") %> prohibited this user_admin from being saved:</h2>

      <ul>
      <% @user_admin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  	<%= @user.firstname + " " + @user.lastname %>
  </div>
  <br />
  <div class="field">
    <%= f.select :level,[["No Admin Priviledges", 0], ["Contributor", 1],["Managing Admin", 2], ["Super Admin", 3]] %>
    <%= f.submit %>
  </div>
<% end %>
