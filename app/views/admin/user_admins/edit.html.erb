<div class="floatbox_trans">
  <h1>Editing user_admin</h1>
  
  <%= render 'form' %>
  
  <%= link_to 'Destroy',[:admin, @user_admin], method: :delete, data: { confirm: 'Are you sure?' } %> |
  <%= link_to 'Back', admin_user_admins_path %>
</div>

