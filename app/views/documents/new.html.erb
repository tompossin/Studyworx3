
  
<div id="content-large" style="height:100%;">  
  <article style="height:100%;">
    <h1><%= "#{@task.assignment.name} - #{@task.name}" %></h1>
    <%=link_to "<#{@task.assignment.name} task list", school_assignment_path(@task.assignment.school_id, @task.assignment_id) %>
    &nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;&nbsp;
    <%=link_to "...Change to HTML Editor...",document_toggle_type_path(@document),{method: "put", title: "Converth this document for use with thes HTML editor."} if @document.content_type == 0 %>
    <%=link_to "...Change to Markdown Editor...",document_toggle_type_path(@document),{method: "put", title: "Save a seperate copy first.", data: { confirm: 'This could cause data loss, save this outside of studyworx first just to be sure.' }} if @document.content_type == 1 %>
    
    <%= render 'form' %>

  </article>
</div>

<%= nav_bar(@nav_body_content)%>

<%= smart_edit(@document.content_type, document_path(@document)) %>
