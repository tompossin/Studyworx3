<!DOCTYPE html>
<html>
<head>
  <title>Studyworx</title>
  <%= stylesheet_link_tag    "base", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div id='content'>
		<%= yield %>
	</div>
	<div id='header'>
		<div id='logo' onclick="ShowHide('logomenu')">
			{:> StudyWorx
			<p id="logomenu" style="display:none">
				<%= link_to "Home Page", root_path %>
			</p>
		</div>
	</div>
	<div id='profile' onclick="ShowHide('profilemenu')">
		<% if current_user %>
		  <%= full_name %>
		<% else %>
		  <%= link_to "Sign_in", new_user_session_path %> or <%=link_to "Sign_up", new_user_registration_path%>
		<% end %>
		<p id="profilemenu" style= "display:none">
			<%= link_to "Edit Profile", edit_user_registration_path if current_user %><br />
			<%= link_to "Log Out", destroy_user_session_path, :method=>:delete if current_user %><br />
		</p>
	</div>

	<div id="notice" onclick="ShowHide('notice')" ><%= notice + %{ (click to hide)} if notice %></div>
	<div id="alert" onclick="ShowHide('alert')" ><%= alert + %{ (click to hide)} if alert %></div>
	
</body>
</html>
