
  <%= form_tag({:controller=>:profiles,:action=>:save_reminder}, {:remote=> true, :id=>current_user.id}) do |r| %>
    <%= text_area_tag :reminder, @note.reminder, {:size=>"34x5"} %><br />
    <%= submit_tag "save" %>
  <% end %>

