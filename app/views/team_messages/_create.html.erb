<%= form_tag new_team_message_path, {remote: true, method: :get} %>
  <%= select "message", "team_id", current_user.teams.all.collect {|t| [ t.name, t.id ] }, prompt: "Select a Team" %>
  <%= submit_tag "Send" %>

